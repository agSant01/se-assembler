<UserControl x:Class="Simulator_UI.SevenSegmentDisplay"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:Simulator_UI"
             mc:Ignorable="d" 
             d:DesignHeight="200" d:DesignWidth="120">
  <UserControl.Resources>
    <local:BoolToVisibilityConverter x:Key="segmentConverter"/>
    <local:BoolToColorConverter x:Key="segmentColorConverter"/>
  </UserControl.Resources>

  <Grid>
    <Grid.RowDefinitions>
      <RowDefinition/>
    </Grid.RowDefinitions>
    <Grid.ColumnDefinitions>
      <ColumnDefinition/>
      <ColumnDefinition/>
      <ColumnDefinition/>
    </Grid.ColumnDefinitions>

    <Grid x:Name="DigitOne" Grid.Column="0">
      <Grid.RowDefinitions>
        <RowDefinition/>
        <RowDefinition Height="15"/>
        <RowDefinition/>
      </Grid.RowDefinitions>
      <Grid.ColumnDefinitions>
        <ColumnDefinition/>
      </Grid.ColumnDefinitions>

      <DockPanel x:Name="FirstNumTop" Grid.Column="0" LastChildFill="False">
        <StackPanel Background="{Binding IsFirstDigitActive, Converter={StaticResource segmentColorConverter}}" Visibility="{Binding FirstNumber[0], Converter={StaticResource segmentConverter}}" Height="15" DockPanel.Dock="Top"/>
        <StackPanel Background="{Binding IsFirstDigitActive, Converter={StaticResource segmentColorConverter}}" Visibility="{Binding FirstNumber[1], Converter={StaticResource segmentConverter}}" Width="15" DockPanel.Dock="Right"/>
        <StackPanel Background="{Binding IsFirstDigitActive, Converter={StaticResource segmentColorConverter}}" Visibility="{Binding FirstNumber[5], Converter={StaticResource segmentConverter}}" Width="15" DockPanel.Dock="Left"/>
      </DockPanel>
      <DockPanel x:Name="FirstNumMid" Grid.Column="0" Grid.Row="1">
        <StackPanel Background="{Binding IsFirstDigitActive, Converter={StaticResource segmentColorConverter}}" Visibility="{Binding FirstNumber[6], Converter={StaticResource segmentConverter}}" DockPanel.Dock="Left"/>
      </DockPanel>
      <DockPanel x:Name="FirstNumBot" Grid.Column="0" Grid.Row="2" LastChildFill="False">
        <StackPanel Background="{Binding IsFirstDigitActive, Converter={StaticResource segmentColorConverter}}" Visibility="{Binding FirstNumber[3], Converter={StaticResource segmentConverter}}" Height="15" DockPanel.Dock="Bottom"/>
        <StackPanel Background="{Binding IsFirstDigitActive, Converter={StaticResource segmentColorConverter}}" Visibility="{Binding FirstNumber[2], Converter={StaticResource segmentConverter}}" Width="15" DockPanel.Dock="Right"/>
        <StackPanel Background="{Binding IsFirstDigitActive, Converter={StaticResource segmentColorConverter}}" Visibility="{Binding FirstNumber[4], Converter={StaticResource segmentConverter}}" Width="15" DockPanel.Dock="Left"/>
      </DockPanel>

    </Grid>

    <StackPanel x:Name="Divider" Grid.Column="1" Margin="10" Background="LightGray" Width="2" />

    <Grid x:Name="DigitTwo" Grid.Column="2">
      <Grid.RowDefinitions>
        <RowDefinition/>
        <RowDefinition Height="15"/>
        <RowDefinition/>
      </Grid.RowDefinitions>
      <Grid.ColumnDefinitions>
        <ColumnDefinition/>
      </Grid.ColumnDefinitions>


      <DockPanel x:Name="SecondNumTop" Grid.Column="0" LastChildFill="False">
        <StackPanel Background="{Binding IsSecondDigitActive, Converter={StaticResource segmentColorConverter}}" Visibility="{Binding SecondNumber[0], Converter={StaticResource segmentConverter}}" Height="15" DockPanel.Dock="Top"/>
        <StackPanel Background="{Binding IsSecondDigitActive, Converter={StaticResource segmentColorConverter}}" Visibility="{Binding SecondNumber[1], Converter={StaticResource segmentConverter}}" Width="15" DockPanel.Dock="Right"/>
        <StackPanel Background="{Binding IsSecondDigitActive, Converter={StaticResource segmentColorConverter}}" Visibility="{Binding SecondNumber[5], Converter={StaticResource segmentConverter}}" Width="15" DockPanel.Dock="Left"/>
      </DockPanel>
      <DockPanel x:Name="SecondNumMid" Grid.Column="0" Grid.Row="1">
        <StackPanel Background="{Binding IsSecondDigitActive, Converter={StaticResource segmentColorConverter}}" Visibility="{Binding SecondNumber[6], Converter={StaticResource segmentConverter}}" DockPanel.Dock="Left"/>
      </DockPanel>
      <DockPanel x:Name="SecondNumBot" Grid.Column="0" Grid.Row="2" LastChildFill="False">
        <StackPanel Background="{Binding IsSecondDigitActive, Converter={StaticResource segmentColorConverter}}" Visibility="{Binding SecondNumber[3], Converter={StaticResource segmentConverter}}" Height="15" DockPanel.Dock="Bottom"/>
        <StackPanel Background="{Binding IsSecondDigitActive, Converter={StaticResource segmentColorConverter}}" Visibility="{Binding SecondNumber[2], Converter={StaticResource segmentConverter}}" Width="15" DockPanel.Dock="Right"/>
        <StackPanel Background="{Binding IsSecondDigitActive, Converter={StaticResource segmentColorConverter}}" Visibility="{Binding SecondNumber[4], Converter={StaticResource segmentConverter}}" Width="15" DockPanel.Dock="Left"/>
      </DockPanel>
    </Grid>
  </Grid>

</UserControl>